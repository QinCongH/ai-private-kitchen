# 应用配置
APP_NAME=Agent Backend
APP_VERSION=0.1.0
DEBUG=false

# API 配置
API_PREFIX=/api/v1
API_HOST=0.0.0.0
API_PORT=8000

# CORS 配置
CORS_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]
